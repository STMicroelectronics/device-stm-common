# initprop executable
type initprop, domain;
type initprop_exec, exec_type, vendor_file_type, file_type;

init_daemon_domain(initprop)

# initprop use cmdline to set proprietary properties
allow initprop proc_cmdline:file { r_file_perms };

set_prop(initprop, vendor_initprop_prop)

