# Patch list definition (for each git repository which shall be set and/or patched):

# PATCH_CONFIG_VERSION to be increased for each change within this file

# PATCH_PATH <path> (relative path of the patched directory from Android top directory)
# PATCH_COMMIT <commit sha1> (specific version required)
# PATCH_1 <file name> (file name without .patch suffix)
# ...
# PATCH_<n> <file name> (file name without .patch suffix)

# Increase <n> value by 1 to show order (add it in ascending order)
# Patch files to be added in device/stm/stm32mp1/patch/android/

# version of the patch configuration required
PATCH_CONFIG_VERSION 2

# ===================================== #
# Three types of patches:
# Patch to ... : mandatory patches
# Temporary patch to ... : mandatory patches (issue tracker opened, wait for official correction)
# Optional patch to ... : optional patches (can be removed taking care of dependencies)
# ===================================== #

# ===================================== #
# Android patch to external             #
# ===================================== #

# -------------------------------------
PATCH_PATH external/drm_hwcomposer

# Select required drm_hwcomposer version (do not modify)
PATCH_COMMIT 68b94c0faebf0bf2d024fab7ed812e706589bbf1
PATCH_1 drm_hwcomposer/0001-drm_hwcomposer-Remove-error-log-linked-to-layer-impo
PATCH_2 drm_hwcomposer/0002-drm_hwcomposer-Insure-that-the-layer-is-compatible-w
PATCH_3 drm_hwcomposer/0003-drm_hwcomposer-Remove-minigbm-missing-dependency-tem
PATCH_4 drm_hwcomposer/0004-drm_hwcomposer-Avoid-double-hotplug-call-for-primary
PATCH_5 drm_hwcomposer/0005-update-property-name-associated-to-vendor-namespace
PATCH_6 drm_hwcomposer/0006-Check-vsync-state-just-before-calling-the-callback-r

# -------------------------------------
PATCH_PATH external/blktrace

# Select required blktrace version (do not modify)
PATCH_COMMIT c80018f8775ae62741aff7741038547cfc5ff409

# ===================================== #
# Android patch to build                #
# ===================================== #

# -------------------------------------
PATCH_PATH build/make

# Optional patch to integrate CoproService
PATCH_1 build_make/0001-Integration-of-Copro

# Temporary patch to solve kernel config extract script (waiting official correction)
PATCH_2 build_make/0002-Correct-script-to-extract-kernel-info-waiting-offici

# Patch to avoid integrating the default go handheld permissions which are not supported by the hardware
PATCH_3 build_make/0003-Remove-default-permission-configuration-for-a-go-han

# Optional patch to remove unnecessary packages from build
PATCH_4 build_make/0004-Remove-unnecessary-packages

# ===================================== #
# Android patch to hardware             #
# ===================================== #

# -------------------------------------
PATCH_PATH hardware/interfaces

# Optional patch to integrate CoproService
PATCH_1 hardware_interfaces/0001-Integration-of-Copro-HAL

# Temporary patch to remove security requirement
PATCH_2 hardware_interfaces/0002-temporary-disable-security-requirements-on-compatibi

# Temporary patch to remove minijail configuration for configstore (seccomp policy not defined for ARM32)
PATCH_3 hardware_interfaces/0003-temporary-disable-minijail-configuration-for-configs

# Optional patch to support char[] data format in CoproService
PATCH_4 hardware_interfaces/0004-copro-add-readB-and-writeB-to-support-char-data-form

# Optional patch to treat layer count in wrapper as not managed in gralloc0
PATCH_5 hardware_interfaces/0005-gralloc0-module-can-t-treat-layerCount-test-shall-be

# ===================================== #
# Android patch to frameworks           #
# ===================================== #

# -------------------------------------
PATCH_PATH frameworks/av

# Optional patch to allow decoding video without stride metadata (use width as backup)
PATCH_1 frameworks_av/0001-Use-width-as-stride-backup-instead-of-generating-an-

# Optional patch to disable logs in frameworks
PATCH_2 frameworks_av/0002-Disable-frameworks-logs-by-default

# -------------------------------------
PATCH_PATH frameworks/base

# Optional patch to integrate CoproService
PATCH_1 frameworks_base/0001-Integration-of-Copro

# Temporary patch to bypass ReliableSurface generating freeze on low-end devices (see opened issue b144367852)
PATCH_2 frameworks_base/0002-Bypass-ReliableSurface-generating-display-freeze-on-

# Optional patch to remove series of StrictMode violations
PATCH_3 frameworks_base/0003-Avoid-unnecessary-StrictMode-violation

# Optional patch to avoid error in logcat concerning the WindowManager (no real impact)
PATCH_4 frameworks_base/0004-Add-image-directory-creation-to-avoid-error-to-acces

# Optional patch to avoid error in logcat concerning the ActivityManager (no real impact)
PATCH_5 frameworks_base/0005-Remove-error-in-ActivityManager

# Temporary patch to allow generating thumbnails correctly (see opened issue b144301466)
PATCH_6 frameworks_base/0006-Correct-ThumbnailUtils-createVideoThumbnail-function

# Optional patch to avoid error in logcat concerning the MediaMetadataRetriever (no real impact)
PATCH_7 frameworks_base/0007-Set-retriever-only-if-not-null-value

# Optional patch (boot time) to remove TelecomService (dependecy with packages/apps/Settings and build/make patch)
PATCH_8 frameworks_base/0008-Remove-TelecomService

# Optional patch (boot time) to reduce the number preloaded classes
PATCH_9 frameworks_base/0009-Reduce-number-of-preloaded-classes

# Optional patch (boot time) to reduce the number of fonts
PATCH_10 frameworks_base/0010-Reduce-number-of-fonts

# Optional patch to change the boot message (if displayed)
PATCH_11 frameworks_base/0011-change-boot-message

# Optional patch to disable logs in frameworks
PATCH_12 frameworks_base/0012-Disable-logs-by-default

# -------------------------------------
PATCH_PATH frameworks/native

# Optional patch to adapt fake vsync to default display configuration (50Hz)
PATCH_1 frameworks_native/0001-Set-fake-vsync-at-50Hz-instead-of-60Hz

# Patch to increase timeout to access vendor service (required during boot)
PATCH_2 frameworks_native/0002-Increase-interval-between-two-vendor-service-request

# Patch to increase timeout to waif for vsync start (required during boot)
PATCH_3 frameworks_native/0003-Increase-timeout-for-vsync-start-during-boot

# Optional patch to avoid event logs overflow
PATCH_4 frameworks_native/0004-Increase-EventLog-storage-size-to-avoid-overflow

# Optional patch to force initialization (not always well performed when protected by mutex)
PATCH_5 frameworks_native/0005-EventThread-force-state-initialization

# Optional patch to disable logs in frameworks
PATCH_6 frameworks_native/0006-Disable-frameworks-logs-by-default

# -------------------------------------
PATCH_PATH frameworks/opt/net/wifi

# Patch to add temporarily libwifi vendor library for STM
PATCH_1 frameworks_opt_net_wifi/0001-Add-libwifi-hal-stm-vendor-specific-library-inclusio

# -------------------------------------
PATCH_PATH frameworks/opt/telephony

# Temporary patch to avoid ArrayIndexOutOfBoundsException (waiting official correction)
PATCH_1 frameworks_opt_telephony/0001-Check-phoneId-value-to-avoid-ArrayIndexOutOfBoundsEx

# ===================================== #
# Android patch to packages             #
# ===================================== #

# -------------------------------------
PATCH_PATH packages/apps/Bluetooth

# Optional patch to increase start timeout
PATCH_1 packages_apps_bluetooth/0001-adapterState-increase-the-timeout-values-for-the-Blu

# -------------------------------------
PATCH_PATH packages/apps/Settings

# Optional patch to remove TelecomService (dependecy with frameworks/base and build/make patch)
PATCH_1 packages_apps_settings/0001-Remove-TelecomService-dependency

# Optional patch to simplify fallback home (remove boot message)
PATCH_2 packages_apps_settings/0002-Simplify-FallbackHome-activity-remove-animation

# Optional patch to allow overlay activity in lowram device (demo purpose)
PATCH_3 packages_apps_settings/0003-Allow-overlay-for-lowram-devices-demo-purpose

# -------------------------------------
PATCH_PATH packages/providers/MediaProvider

# Temporary patch to correct external primary management (waiting official correction)
PATCH_1 packages_providers_mediaprovider/0001-Return-external_primary-volume-name-for-primary-stor

# ===================================== #
# Android patch to development          #
# ===================================== #

# -------------------------------------
PATCH_PATH development

# Patch to solve SDK build issue
PATCH_1 development/0001-Update-for-SDK-generation-with-aosp_xxx-build

# ===================================== #
# Android patch to system               #
# ===================================== #

# -------------------------------------
PATCH_PATH system/sepolicy

# Optional patch to integrate CoproService
PATCH_1 system_sepolicy/0001-Update-system-sepolicy-for-CoproService
